#include "main.h"
/**
 * rev_string - function
 * @s: check input
 * Return: void
*/
void rev_string(char *s)
{
int i;
int len = 0;
for (i = 0; s[i] != '\0'; i++)
{
len++;
}
for (i = len; i >= 0; i--)
{
}
}
